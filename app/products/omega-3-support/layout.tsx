import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Heyo Superfood Dog Treats - Omega-3 Support Bites",
  description: "Essential fatty acids blend for brain health and joint support.",
};

export default function Omega3SupportLayout({ children }: { children: React.ReactNode }) {
  return children;
}
